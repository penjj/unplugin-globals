/* eslint-disable eslint-comments/no-unlimited-disable */
/* eslint-disable */
import { h, e } from "lib-a";
import * as all from "lib-b";

import { o } from "lib-a";
import value, { d } from "lib-b";

const ret = h.toUpperCase() +
  e +
  all.l +
  all.l +
  o +
  ' ' +
  value.w.toUpperCase() +
  o +
  value.r +
  all.l +
  d;
